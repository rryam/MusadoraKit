workflows:
  musadorakit-test-workflow:
    name: MusadoraKit Testing Workflow
    instance_type: mac_mini
    environment:
      xcode: latest
    triggering:
      events:
        - push
    scripts:
      - name: Test
        script: |
          swift test
